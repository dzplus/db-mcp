[project]
name = "db-mcp"
version = "0.1.0"
description = "MySQL数据库对接的MCP服务器"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "mysql-connector-python",
    "python-dotenv"
]